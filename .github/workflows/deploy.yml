name: Deploy from QA Twin

on:
  repository_dispatch:
    types: [deploy-from-qa-twin]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run a simple deployment step
        run: |
          echo "🚀 Deployment triggered by QA Twin!"
          echo "Environment: ${{ github.event.client_payload.environment }}"
          # Add your actual deployment commands below.
          # For example, if you have a deploy script:
          # npm install && npm run deploy
